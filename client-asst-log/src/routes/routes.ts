export enum ROUTES {
  LOGIN = "/login",
  REGISTER = "/register",
  HOME = "/",
  DASHBOARD = "/dashboard",
  ATTENDANCE_GROUP = "/attendance-group",
  ATTENDANCE = ":attendance",
  SETTINGS = "/settings",
  USERS = "/users",
  NOT_FOUNT = "*",
}
